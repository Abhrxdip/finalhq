name: Pull Request validation

on: [pull_request]


jobs:
  HackQuest-contracts-ci:
    name: Run HackQuest-contracts validation
    uses: ./.github/workflows/HackQuest-contracts-ci.yaml

  HackQuest-frontend-ci:
    name: Run HackQuest-frontend validation
    uses: ./.github/workflows/HackQuest-frontend-ci.yaml

